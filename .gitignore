# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# Environment / secrets
.env
*.key

# Data — raw pulls and large caches are not committed.
# Small public fixtures used for tests are kept (force-added or under tests/fixtures/).
data/raw/
data/processed/
*.parquet
*.feather
!tests/fixtures/**

# Models & reports (regenerated by `make`) — anchored to repo root so they do
# NOT match the source package src/fifa2026/models/ or any nested dir.
/models/
/reports/

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
